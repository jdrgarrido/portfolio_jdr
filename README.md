1 # 🚀 Mi Portfolio Interactivo 3D

Este proyecto es un experimento personal para crear un portfolio web inmersivo utilizando **Three.js** y **Vanilla JS**. El objetivo es combinar ingeniería de software con diseño creativo.

## 🛠️ Tech Stack
- **Motor 3D:** Three.js (Vanilla)
- **Build Tool:** Vite
- **Lenguaje:** JavaScript (ES6+)

## 📓 Devlog (Bitácora de Desarrollo)

### [Fecha de Hoy] - Fase 1: El Setup
- Inicialización del proyecto con Vite (Vanilla template).
- Configuración de la escena base (Scene, Camera, Renderer).
- Implementación del primer objeto: Un `TorusKnot` con wireframe girando.
- **Aprendizaje clave:** Entendiendo el loop de renderizado `requestAnimationFrame`.

## 📦 Instalación y Uso

1. Clonar el repositorio:
    ```bash
    git clone https://github.com/jdrgarrido/portfolio_jdr.git
    ```

2. Instalar dependencias:
    ```bash
    npm install
    ```

3. Correr servidor de desarrollo:
    ```bash
    npm run dev
    ```

### ¿Por qué esto es valioso?
Cuando un reclutador vea tu GitHub, verá la sección **Devlog** y entenderá **cómo piensas y cómo resuelves problemas**, no solo el código final. A medida que avance el proyecto iré anotando decisiones de UX (avatar, navegación con hotspots), optimizaciones y aprendizajes.